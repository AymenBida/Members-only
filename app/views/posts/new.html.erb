
<%= form_with model: @post do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body, 'Add a new post here' %>
  <%= f.text_area :body %>

  <%= f.submit 'Submit' %>


<% end %>

